apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: backend-user
  name: backend-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-user
  template:
    metadata:
      labels:
        app: backend-user
    spec:
      containers:
      - image: mo3bdlaa/udagram-api-user:v6
        name: backend-user
        imagePullPolicy: Always
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: env-secret
              key: POSTGRES_PASSWORD
        - name: POSTGRES_USERNAME
          valueFrom:
            configMapKeyRef:
              name: env-config
              key: POSTGRES_USERNAME
        - name: URL
          valueFrom:
            configMapKeyRef:
              name: env-config
              key: URL
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: env-secret
              key: JWT_SECRET
        - name: POSTGRES_DATABASE
          valueFrom:
            configMapKeyRef:
              name: env-config
              key: POSTGRES_DB
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: env-config
              key: POSTGRES_HOST
      restartPolicy: Always

# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: user-deployment
#   labels:
#     app: user
# spec:
#   replicas: 3
#   selector:
#     matchLabels:
#       app: user
#   template:
#     metadata:
#       labels:
#         app: user
#     spec:
#       containers:
#       - name: user
#         image: mo3bdlaa/udagram-api-user:v5
#         ports:
#         - containerPort: 5432
#       resources:
#         requests:
#           memory: "64Mi"
#           cpu: "250m"
#         limits:
#           memory: "1024Mi"
#           cpu: "500m"